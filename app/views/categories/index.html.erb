<p style="color: green"><%= notice %></p>

<% content_for :title, "Categories" %>

<h1>Categories</h1>
<div class="category_btn_new">
  <%= link_to "New category", new_category_path %>
</div>

  <% @categories.each do |category| %>
<div id="categories" class="category_container">
    


    <!-- Let's just save these lines for later... -->
    <%#= render category %>
    <!-- End of pre-created lines -->

  <div class="category_name" style="background-color: <%= category.color %> ">
    <div class="category_name_text">
      <%= category.name %>
    </div>
    <div class="category_name_color">
      <%= category.color %>
    </div>
  </div>
  <div class="category_show">
    <%= link_to "Show this category", category %>
  </div>
</div>
  <% end %>

